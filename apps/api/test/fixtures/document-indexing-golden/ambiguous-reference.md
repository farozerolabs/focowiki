# Ambiguous Reference

This file refers to Shared Topic and the common-topic alias without an explicit
path. Because two current files provide both identities, no file relationship
may be bound to an arbitrary target.
