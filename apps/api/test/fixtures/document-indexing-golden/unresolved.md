# Unresolved Reference

The referenced [future runbook](future/runbook.md) is intentionally absent.
The reference must remain durable and resolve only after an unambiguous target
with that identity becomes active.
